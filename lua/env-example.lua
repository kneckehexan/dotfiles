-- GOOGLE_* values are used for Calendar.vim. To obtain, follow instructions in ":h calendar" (https://github.com/itchyny/calendar.vim)

local env = {
	WIKI_PATH = "/path/to/path",
	GOOGLE_API = "longapikey",
	GOOGLE_CLIENT_ID = "longgoogleoauthclientid",
	GOOGLE_CLIENT_SECRET = "secret",
}

return env
